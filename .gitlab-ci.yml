image: raatiniemi/gradle:latest

stages:
  - test
  - build

test:
  stage: test
  script:
    - gradle test
  tags:
    - docker

build:
  stage: build
  script:
    - ./bin/build
  tags:
    - docker
